name: Deploy PROD

on:
  workflow_call:

    inputs:

      repository:
        required: true
        type: string

      environment:
        required: true
        type: string

jobs:

  deploy_prod:

    uses: ./.github/workflows/reusable-deploy.yml

    secrets: inherit

    with:

      repository: ${{ inputs.repository }}

      environment: PROD
