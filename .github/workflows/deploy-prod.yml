name: Deploy PROD

on:
  workflow_call:

    inputs:

      repository:
        required: true
        type: string

      script:
        required: true
        type: string

jobs:

  deploy_prod:

    uses: ./.github/workflows/reusable-deploy.yml

    secrets: inherit

    with:

      repository: ${{ inputs.repository }}

      environment: PROD

      script: ${{ inputs.script }}
